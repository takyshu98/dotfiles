# Hunk skill

diffの確認やコードレビューを行うタスクでは、まず `hunk skill path` を実行してHunk CLIのスキルファイルの場所を取得し、その内容を読み込んで従うこと。ライブのHunkセッションが存在する場合は、`git diff`を直接読むだけでなく、そのスキルの案内に従って `hunk session *` コマンドでハンクの確認・ナビゲート・コメント追加を行う。
